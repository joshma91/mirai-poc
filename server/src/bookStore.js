const addBook = ({ title, id }) => {
  // TODO - actually add book to Firebase
  return true;
};

const getBook = id => {
  // TODO - actually get book from Firebase
  return { title: "Placeholder", id: "PLACEHOLDER_ID" };
};

module.exports = { addBook, getBook };
